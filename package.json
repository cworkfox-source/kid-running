{
  "name": "kid-running",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "start": "node server.js",
    "emulators": "npx --yes firebase-tools@13.29.3 emulators:start --only auth,firestore",
    "test:emulator": "npx --yes firebase-tools@13.29.3 emulators:exec --project demo-kid-running --only firestore \"node --test --test-concurrency=1 test/emulator.test.js\""
  }
}
