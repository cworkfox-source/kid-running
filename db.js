let database;
export async function openDB(){return new Promise((resolve,reject)=>{const req=indexedDB.open('kid-running',1);req.onupgradeneeded=()=>{for(const name of ['records','children','settings'])req.result.createObjectStore(name,{keyPath:'id'});};req.onsuccess=()=>{database=req.result;database.onversionchange=()=>database.close();resolve(database);};req.onerror=()=>reject(req.error);req.onblocked=()=>reject(Error('請關閉其他分頁後重試'));});}
export function all(store){return new Promise((resolve,reject)=>{const req=database.transaction(store).objectStore(store).getAll();req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error);});}
export function write(changes){return new Promise((resolve,reject)=>{const tx=database.transaction([...new Set(changes.map(c=>c.store))],'readwrite');tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error);tx.onabort=()=>reject(tx.error||Error('儲存取消'));for(const c of changes){const store=tx.objectStore(c.store);if(c.clear)store.clear();else if(c.delete)store.delete(c.delete);else store.put(c.value);}});}
